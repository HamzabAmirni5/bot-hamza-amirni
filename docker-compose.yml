version: '3.8'

services:
  bot:
    build: .
    volumes:
      - ./session:/app/session
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "3000:3000"
    restart: always
